# ADRs

Os ADRs deste diretório registram decisões de arquitetura do MVP e, quando necessário, distinguem:

- a direção decidida para o produto;
- o que já está implementado no código;
- o que continua como roadmap.

Estado atual dos ADRs:

- `0001-single-binary-mvp.md`: aceito e implementado parcialmente.
- `0002-local-sdk-evaluation.md`: aceito como direção arquitetural; implementação ainda pendente.
- `0003-postgres-plus-redis.md`: aceito e implementado parcialmente.

Regra para este workspace: atualizar ADR apenas quando a decisão mudar ou quando o estado de implementação da decisão precisar ser corrigido para refletir o código real.
